import classNames from "classnames/bind";
import styles from "./styles.module.scss";
const cn = classNames.bind(styles);

const ProductPage = () => {
   
   return <div className={cn("product")}>ProductPage</div>;
};

export default ProductPage;
